# Track A expansion execution plan

Status: execution plan and evidence matrix for the owner-authorized September 10 backend expansion. This document does not authorize production writes, UI changes, external feeds, monitoring jobs, or deployment. Staging changes require explicit project targeting and empirical verification.

## Evidence baseline

- Canonical checkout: `/Users/tsconnely/dev/element10-app`, branch `foundation-a6`.
- Tenant engine: A6a through A7 are implemented in migrations `20260717120000` through `20260731120000` and accepted on staging. Production remains pre-tenant and parked.
- Existing shared catalog: `e10_cards`, `e10_checklists`, `e10_players`, `e10_sets`, and `e10_teams`; A6c.4 makes them member-readable and tenant-mutation deny-by-default.
- Existing tenant inventory: `e10_inventory_items`, movements, reservations, and idempotency receipts. It has no stable product/configuration, supplier, purchasing, lot, physical-copy, listing, customer-transaction, or commercial-event relations.
- Existing market observations: `e10_obs_product_prices` is organization-private competitive evidence, not a shared feed or complete transaction model.
- Accepted constraints: `docs/EXPANSION_FRAMEWORK.md`, `docs/EXPANSION_ACCEPTANCE_CASES.md`, `docs/DOMAIN_MAP.md`, `docs/SECURITY.md`, and the approved product-supply invariants.

## Requirement-to-existing-system matrix (historical baseline before TA-X1)

| Requirement | Status | Existing evidence | Required backend delta | Dependency or warning |
|---|---|---|---|---|
| Explicit tenant authority | Verified on staging | `e10_organizations`, memberships, roles, entitlements, `e10.*` predicates, A6c policies | Reuse explicit organization arguments and predicates | Never infer the first membership; production is not migrated |
| Stable product master | Missing | Inventory carries denormalized text; catalog card rows are platform references | Org-owned product master with stable UUID and lifecycle status | Product variant and packaging must remain separate |
| Product configuration and version | Missing | Prototype contracts only | Org-owned configuration plus immutable version rows and identifiers | Historical commercial lines bind a version, not mutable defaults |
| Subject/player identity | Partial | `e10_players` has UUID, name, aliases, sport | Add governed provider mappings and time-aware affiliations later | Existing player rows remain platform catalog references |
| Release/set identity | Partial/conflicting | `e10_sets` has UUID, name, year, sport, brand | Add release identity precision and aliases without destructive replacement | Do not use the PF-M5 heuristic; it omits year |
| Catalog variant identity | Missing | `e10_cards` mixes catalog and copy fields, including `serial` | Separate release-scoped variant and many-to-many subjects | Serial numerator belongs to a physical copy |
| Physical unique copy | Missing | Inventory item text fields approximate copies | Org-owned unique-item record linked to inventory and optional catalog variant | Cards-off must support a non-card fixture |
| Locations and purchasing destinations | Missing | No location relation in the schema snapshot | Active org locations plus permission-scoped eligible-destination query | Location policy is a prerequisite for PO commit |
| Suppliers and offerings | Missing | No supplier relation | Supplier, exact configuration offering, vendor code and currency | Internal IDs, barcodes and vendor codes stay distinct |
| PO, invoice, receipt and credit | Missing | `e10_mutation_receipts` is only an idempotency ledger | Separate versioned commercial objects, lines and allocations | Invoice approval never creates stock; no fabricated PO |
| Last actual purchase cost | Missing | Item `cost` and movement JSON lack eligible historical lookup contract | Derive from accepted receipt/lot evidence with source/date/currency | Unreceived PO price is not last paid; overrides persist |
| Internal/vendor comments | Prototype-only | Memory-only September candidate | Separate org-private comment classes and allowlisted output projection | Internal comments must never enter vendor output |
| Lots and landed cost | Missing | Inventory movements exist, no lot relation | Receipt-linked lots, accepted quantity, quarantine and additive adjustments | Preserve no-item-FK ledger history |
| Reservations against supply | Partial | Show-ref item reservations exist | Add lot-backed confirmed reservations and expected-supply allocations | No overcommit; concurrency boundary is mandatory |
| Idempotent intake and evidence | Partial | Org-scoped mutation receipts exist | Typed intake batch/row staging, fingerprints, review state and corrections | Manual, CSV and native routes use one resolver |
| Commercial lifecycle history | Missing | Inventory movement types cover quantity changes only | Typed immutable events with source, event time, ingest time and correction lineage | Events are atomic with state changes or an outbox |
| Customer retail and break activity | Partial | Break slots/viewers exist; no official customer transaction model | Provisional observations separate from reviewed posted transactions and adjustments | Presence is not watch time; imports must reconcile, not duplicate |
| Reporting and screener | Partial | Bounded inventory reads and catalog indexes exist | Permission-scoped typed query functions, explicit grain, filters, grouping, cursor and drill-down | Apply row filters before aggregation; local data must suffice |
| Valuation, grading and population history | Missing/partial | Catalog grade fields are denormalized | Typed assessments, grade events, population snapshots and versioned valuation runs | Acquisition basis and appreciation are separate |
| AI query/action seam | Missing | No bounded tool contract | Read-only sourced queries and reviewable command drafts using ordinary authorization | No arbitrary SQL, superuser, chatbot or silent action |
| Wishlist and monitoring | Missing/deferred | No relations | Wishlist can be added independently; monitor rules remain deferred | Saving a wishlist item must not schedule a monitor |

## Dependency-ordered execution batches

1. **TA-X1, identity foundation.** Add organization product master, configuration/version, release-scoped catalog variant identity, variant subjects, provider mappings, and generic physical unique-item identity. Preserve existing catalog and inventory tables. Prove tenant isolation, copy-versus-variant separation, multi-subject identity, Cards-off use, and born-locked functions.
2. **TA-X2, location, supplier and offering foundation.** Add active organization locations, location grants, suppliers, supplier offerings and exact configuration identifiers. Add bounded permission-checked reads. Do not yet create accounting events.
3. **TA-X3, purchasing documents and comments.** Add PO, invoice, credit and receipt headers/lines/revisions, explicit allocations, separate internal/vendor comments, source-document identity and duplicate fingerprints. Preserve distinct approval, receipt and payment events.
4. **TA-X4, receipt, lot and reservation writers.** Add accepted/quarantined quantities, lots, landed-cost evidence, expected allocations and lot-backed confirmed reservations. Implement compare-and-set or locking proofs, idempotent RPCs, partial receipt, reversal and no-overcommit tests.
5. **TA-X5, typed intake and lifecycle.** Add intake batches/rows, resolver decisions, immutable commercial events and correction lineage. Connect native/manual/import actions atomically or through a transactional outbox.
6. **TA-X6, customer reconciliation.** Add provisional break/retail observations, reviewed posted transactions, refunds and source reconciliation. Prove that later imports link rather than double-count and that presence remains source-labeled.
7. **TA-X7, reporting contracts.** Add bounded permission-scoped queries with explicit scope/grain, full-dataset filtering, grouping, sorting, cursor pagination, source drill-down and metric definitions. Add grading/population/valuation provenance where the underlying facts exist.
8. **TA-X8, future integration seams.** Add read-only query contracts and reviewable action-draft records. Wishlist may be added independently. Live integrations, scheduled monitors, chatbot behavior and collector showcase remain outside scope.

## Execution evidence, current at `f38fe05`

| Batch | State | Evidence now present | Required remainder |
|---|---|---|---|
| TA-X1 | Partial, implemented foundation CI/staging verified | Stable org product/configuration/version identity, release variants and subjects, provider mappings, generic physical copies; Cards-off and hostile-tenant proofs | Governed dated player-affiliation history storage and query remain required; actual canonical merge/curation and provider-licensing decisions may require owner direction |
| TA-X2 | Implemented, CI/staging verified | Org locations and grants, suppliers, configuration-specific offerings, bounded eligible-destination read, predicate hardening | None for the approved foundation slice |
| TA-X3 | Partial, schema foundation CI/staging verified | Separate purchasing headers/lines/allocations/comments, source deduplication, immutable document revisions and fail-closed actual-cost evidence | PO create/amend/approve/revision writers, audience-safe comment output, and invoice/credit/allocation workflow contracts remain required; original direct receiving must not fabricate a PO; accounting recognition/payment rules remain out of scope |
| TA-X4 | Partial, core writers CI/staging verified | Lots, expected allocations, confirmed lot reservations, atomic reserve/consume/release, receipt posting/reversal, concurrency and no-overcommit proofs | Direct/no-PO invoice-linked and multi-line receiving/reversal plus inspection/quarantine disposition remain required; owner rulings remain required for over-receipt authority and landed-cost allocation method |
| TA-X5 | Complete through X5i, CI/staging verified | Typed intake and resolver lifecycle; reviewed evidence commit; native receipt/movement event links; immutable manual correction and reviewed re-import supersession lineage; atomic corrected durable-source and explicit source-less multirow imports; eligible-current interpretation; product/configuration/unique-item/catalog-variant observations; sequential and concurrent cycle safety; versioned and typed 20-family event envelope; stable idempotency and hostile-tenant proofs | None for the approved foundation slice |
| TA-X6 | Partial, X6a-X6c CI/staging verified | Org-owned customer records; immutable provisional activity; reviewed identity/attribution history; versioned mixed-order drafts; separate prepare/approve/post authority; immutable line-level posted spend and event; source/activity deduplication; verified-handle recheck; bounded non-financial lookup; stable retry/CAS/deadlock and hostile-tenant proofs | Refund/cancellation adjustments, customer merge/split, imported/native reconciliation, native slot capture, attendance evidence and reporting-level double-count prevention |
| TA-X7 | Not started | Existing bounded inventory reads only | Permission-scoped reporting/screener contracts, full-set filtering before aggregation, explicit grain, cursor, drill-down, definitions and source lineage; reconstruct corrected listing intervals and prove overlapping-channel exposure union semantics |
| TA-X8 | Not started | Dormant outbox rows only | Bounded read-only AI query contracts, reviewable action drafts, and a replay-safe dormant outbox claim/ack contract; no external dispatch, chatbot or silent execution |

TA-X5e commit `9431eb941e5c910f727163dc4544c33f1c5f1464` passed exact-head CI run `34525759880`, clean local replay, A7 hostile matrix 29/29, default-privilege checks, and explicit staging verification. Staging contains migration `20260910223000`; production remains pre-tenant with 12 migrations through `20260716110000`. The observation supersession relation is immutable and preserves both prior evidence and a reviewed re-import's intake row/commit provenance. It does not, by itself, close the acceptance case for an atomic corrected-file import or reporting eligibility during the interval between commit and reconciliation.

TA-X5f commit `c335884d4e2a4df0ff19adae481981ae9317fa8d` passed exact-head CI run `34527643446`, clean replay, and explicit staging verification at migration `20260910224500`. For CSV/API/native sources with durable provider identity, exact payloads replay, changed payloads form batch lineage, and every matched observation row receives an explicit reviewed `replacement` or `new` classification. Commit and supersession become visible atomically. Stable event identifiers are provider-connection scoped, not organization-global. Source-less/manual multirow corrected-file reconciliation remains unimplemented.

TA-X5g commit `1467c76b8264ff0e6be09c1cfd4f8d503701663a` passed exact-head CI run `34528648460`, clean replay, and explicit staging verification at migration `20260910230000`. Source-less/manual corrected batches require an explicit same-org committed predecessor; attaching that intent invalidates any prior review revision, committed ordinary batches cannot be retrofitted, and exact committed corrected-stage replay is preserved. A two-connection predecessor race proves one winner, a deterministic mismatch loser, and no deadlock. Production remains unchanged.

TA-X5h head `66fafd092e113d5a827f816961ecf942bd19d827` passed exact-head CI run `34530040410`, clean replay, and explicit staging verification at migration `20260910231500`. A market observation may now target one shared catalog variant without creating an organization-owned copy; its evidence row remains organization-scoped. The eligible-current security-invoker view exposes the new target under its existing service-only grant. Two-organization authenticated negatives prove that sharing the catalog ID does not share intake resolution, correction authority, or observation visibility. Sequential multi-hop and simultaneous reverse-link proofs establish cycle-safe immutable supersession lineage with one winner under concurrency. Production remains unchanged.

TA-X5i head `7853da1e86b3f8818aef3713a2be1c65d8e7a30e` passed exact-head CI run `34531464853`, clean replay, and explicit staging verification at migration `20260910233000`. The immutable commercial-event contract now carries a versioned schema, occurrence precision including unknown time, source connection/event identity, correlation/causation, and evidence quality. Typed v1 payload contracts cover all 20 framework event families. Shared insertion guards reject missing, null, empty, wrong-type, negative, nonfinite, and invalid-currency fields; only inventory-movement-linked writers may assert native-system provenance. Competing-channel listing events reference one physical copy, correction lineage is preserved, and source/idempotency concurrency produces one durable event. Authenticated cross-organization writes and direct reads are denied. Staging fixture residue is zero; production remains unchanged.

TA-X5 completion means the approved typed evidence foundation is complete, not that every commercial workflow or metric is implemented. TA-X6 owns atomic provisional-sale, posted-transaction, adjustment, and reconciliation state changes. TA-X7 owns eligible correction interpretation plus listing-interval and overlapping-channel exposure reconstruction. TA-X8 owns a replay-safe dormant outbox consumer contract without enabling external delivery. These assignments preserve the framework acceptance cases rather than treating event-type coverage alone as their completion.

TA-X6a head `89d1733c6cc79b474a76ac8f867ed1b91840855c` passed exact-head CI run `34532734311`, including clean schema replay, A7 hostile isolation, X6a SQL and two-connection idempotency tests, and the default-privilege probe. Staging contains migration `20260910234500`. The new activity relation is immutable provisional evidence and explicitly returns `posted=false`; it does not create recognized spend. Nullable discount, shipping and tax remain unknown rather than being coerced to zero. A customer-less buyer can be retained with an explicit unresolved identity status. The bounded writer inserts the provisional observation and its `sale_committed` evidence event atomically, denies cross-organization identifiers, allows only manual/import evidence, and makes exact retries stable even if a referenced customer is later archived while still rechecking caller authorization. Staging fixture residue is zero. Production remains pre-tenant with 12 migrations through `20260716110000`, 35 items and 41 movements.

TA-X6b head `55f1712e6e21b5308c764c313b2030cd3b211631` passed exact-head CI run `34534228464`, clean replay, focused SQL, two-connection concurrency, X6a regression, A7 and default-privilege checks. Staging contains migration `20260911000000`. Customer create and update/archive operations are permission-checked, idempotent and revision-bound. Channel-account and ordinary alias associations use immutable reviewed decision chains; aliases remain nonunique across customers, opaque provider IDs preserve case, and Whatnot handles use the canonical normalized handle. Activity attribution corrections append history without rewriting source observations or changing revenue. Concurrent customer identity changes serialize the verified-user projection. Every new activity rechecks the effective identity decision and canonical verified claim instead of trusting that projection. The lookup surface is bounded and excludes contact and financial data. The new capability receives no default role grants. Staging residue is zero; production remains unchanged and does not contain the new relations.

TA-X6c head `f38fe05e951342baa581fab8f0a2ba5b01eb6500` passed exact-head CI run `34535906552`, clean replay, focused SQL, two-connection deadlock/idempotency tests, X6b/X5i regressions, A7 and default-privilege checks. Staging contains migration `20260911001500`. Immutable draft revisions preserve mixed retail, break and unclassified lines, independent channel/location/capture-source dimensions, external break-session references and unknown financial components. Imported/manual break lines do not fabricate native sessions. Product/configuration/copy targets are coherently checked. Prepare, approve and post use distinct ungranted-by-default capabilities; reopening preserves approval history and requires fresh review. Approval snapshots exact activity-attribution decisions, and posting serializes against attribution changes before atomically creating immutable posted header/lines and a guarded posting event. Generic event writers cannot forge that event. Deterministically ordered source/activity locks prevent reverse-order deadlocks, and unique constraints prove one contribution per activity/source line across drafts and keys. Posted explicitly does not assert payment, settlement or accounting recognition. Adjustment storage is present but has no mutation API yet. Staging residue is zero; production remains unchanged.

## Verification required for every batch

- New additive migration files only; never edit an applied migration.
- Local clean replay from baseline, batch-specific tests, existing tenant/RLS regressions, and default-privilege probe.
- RLS on every new public table. No policy may rely on `TO authenticated` without an organization/object predicate.
- `SECURITY DEFINER` functions live behind explicit self-authorization, pinned search path and revoked `PUBLIC`/`anon`; grants are allowlisted.
- Two-organization positive and hostile negative cases, including cross-org IDs supplied directly.
- Idempotency retry, mismatch and concurrent-call tests for every mutator.
- Explicit staging project identity check immediately before each remote write, then explicit staging target. Never use bare remote push while linked to production.
- Exact migration ledger, object census, advisors, CI head and production read-only untouched evidence.
- Batch report distinguishes proposed, implemented locally, applied to staging, independently accepted, and absent from production.

## Genuine escalation boundaries

Stop for owner direction rather than inventing: accounting recognition or payment rules; landed-cost allocation method; over-receipt authority; cross-shop data pooling; provider licensing; automatic financial or price publication; capability namespace replacement; or any request to migrate production. Additive identity fields, explicit source provenance, tenant isolation, idempotency, bounded reads and reviewed-draft seams are within the approved technical scope.
